import styled from "styled-components";

const Container = styled.div`
    display: flex;
    flex-direction: column;
    width: 92px;
    height: 100%;
    border-radius: 10px;
`;

const Sidebar = () => {
    return (
        null
    );
}.